{
  "name": "localhost-tunnel",
  "version": "0.2.0",
  "description": "Simple localhost tunnel - expose your local server to the internet",
  "main": "tunnel-client.js",
  "bin": {
    "tunnel": "./tunnel-client.js",
    "tunnel-relay": "./tunnel-relay.js"
  },
  "scripts": {
    "start": "node tunnel-client.js",
    "relay": "node tunnel-relay.js",
    "dev": "npm run relay"
  },
  "keywords": ["tunnel", "localhost", "websocket", "ngrok", "expose"],
  "author": "Hex",
  "license": "MIT",
  "dependencies": {
    "ws": "^8.16.0"
  }
}
